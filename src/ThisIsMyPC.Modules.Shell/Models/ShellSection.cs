namespace ThisIsMyPC.Modules.Shell.Models;

/// <summary>The tab a shell preference belongs to on the Explorer page.</summary>
public enum ShellSection
{
    General,
    FileExplorer,
    Taskbar,
    Desktop,
    StartMenu,
}
